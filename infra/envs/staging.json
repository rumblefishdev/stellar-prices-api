{
  "envName": "staging",
  "awsRegion": "us-east-1",
  "apiGatewayThrottleRate": 50,
  "apiGatewayThrottleBurst": 100,
  "apiGatewayPartnerDailyQuota": 10000,
  "scheduleExpressions": {
    "priceUpdater": "rate(5 minutes)",
    "oracleWatcher": "rate(5 minutes)",
    "assetDiscovery": "rate(1 hour)",
    "cleanup": "cron(0 3 * * ? *)"
  }
}
